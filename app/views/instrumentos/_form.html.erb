<%= form_with(model: @instrumento, local: true) do |form| %>
  <!--<form class="was-validated">-->
  <div class="row">
    <!-- Bloque izquierda -->
    <div class="col-md-6 col-xs-12">

      <!-- Box name -->
      <div class="form-group">
        <%= form.label :nombre %>
        <%= form.text_field :nombre, class:"form-control" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
              <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box name -->

      <!-- Box tipo -->
      <div class="form-group">
        <%= form.label :tipo %>
        <%= form.text_field :tipo, class:"form-control" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
                <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box tipo -->

      <!-- Box tipo -->
      <div class="form-group">
        <%= form.label :idCategoria %>
        <%= form.text_field :idCategoria, class:"form-control",
                            placeholder: "G=1 B=2 Per=3 O=4 T=5 P=6 I=7 Pro=8 C=9 M=10" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
                <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box tipo -->

    </div>
    <!-- Bloque izquierda -->

    <!-- Bloque derecha -->
    <div class="col-md-6 col-xs-12">

      <!-- Box precio -->
      <div class="form-group">
        <%= form.label :precio %>
        <%= form.text_field :precio, class:"form-control" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
                <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box precio -->

      <!-- Box detalles -->
      <div class="form-group">
        <%= form.label :detalles %>
        <%= form.text_field :detalles, class:"form-control" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
                <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box detalles -->

      <!-- Box Imagen -->
      <div class="form-group">
        <%= form.label :image %>
        <%= form.file_field :image, class:"form-control" %>

        <% if @instrumento.errors.any? %>
          <div id="error_explanation" class="error">
            <span>
              Se han encontrado
              <%= pluralize(@instrumento.errors.count, "error") %>:
            </span>
            <ul>
              <% @instrumento.errors.full_messages.each do |msg| %>
                <li> <%= msg %> </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
      <!-- Box Imagen -->

    </div>
    <!-- Bloque izquierda -->

  </div>
  <div class="d-flex justify-content-center mb-2 mt-4">
    <%= form.submit %>
    <%= link_to 'Cancelar', instrumentos_path %>
    <!--<button type="submit" onclick="return validate_register()" class="btn btn-primary">Registrarse</button>-->
  </div>
<% end %>